import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {

    },
    subTitle: {
        color: '#000000',
        marginTop: 40,
        fontSize: 20,
    },
    row: {
        flexDirection: 'row',
        flexWrap: 'wrap'
    },
    emptyItem: {
        textAlign: 'center',
        fontSize: 13,
        marginTop: 24,
        color: 'red'
    }
});

export default styles;
